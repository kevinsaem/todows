# 데이터베이스 접속 URL (SQLite 개발용, 프로덕션에서는 PostgreSQL 사용)
DATABASE_URL=sqlite+aiosqlite:///./todos.db

# 디버그 모드 (프로덕션에서는 반드시 false)
DEBUG=false

# CORS 허용 도메인 (쉼표로 여러 개 지정 가능, 프로덕션에서는 실제 도메인만 허용)
CORS_ORIGINS=http://localhost:8000
